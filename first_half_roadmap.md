# 첫 반년 개발 로드맵

## 목표

miniCV는 핵심 컴퓨터 비전 알고리즘을 C++로 직접 구현하는 이미지 처리 라이브러리이다.

이 기간에는 이미지 처리의 기반을 직접 쌓는다. 이미지 컨테이너, 파일 입출력, 픽셀 연산, 히스토그램, 임계값 처리, 필터링, 엣지 검출, 모폴로지, 객체 영역 분석까지 구현하며 OpenCV가 제공하는 대표 기능들이 내부적으로 어떻게 동작하는지 이해하는 것을 목표로 한다.

## 진행 방식

이 계획은 주 10시간 정도 꾸준히 개발하는 것을 기준으로 26주 동안 진행한다.

4주를 하나의 묶음으로, 앞의 3주는 새 기능을 구현하고 네 번째 주에는 앞서 만든 기능의 오류를 검증하고 미비한 부분을 보완한다. 이때 작은 예제도 함께 만들어 기능이 실제 처리 흐름에 어떻게 연결되는지 확인한다.

마지막 2주에는 전체 기능을 연결하고 예제와 문서를 정리하며 macOS와 Windows에서 같은 방식으로 빌드되는지 확인한다.

구현 난이도나 설계 문제가 드러나면 세부 일정은 조정한다.

## 개발 방향

- OpenCV는 동작 범위를 정하는 기준으로 사용한다.
- 이미지 타입은 8-bit grayscale/RGB로 제한한다.
- 빈 이미지, 크기 불일치, 잘못된 채널 수, 경계 좌표 같은 실패 상황을 구현 초기에 함께 다룬다.
- 각 기능은 테스트뿐 아니라 작은 예제로 연결해 실제 이미지 처리 흐름 속에서 확인한다.
- 예제는 개별 함수 사용법보다 이미지가 단계적으로 처리되는 과정을 보여주는 데 집중한다.

## 26주 계획

### 1단계: 이미지 기반 구조

| 주차 | 주제 | 핵심 내용 |
| ---: | --- | --- |
| 1 | 이미지 컨테이너 1차 구현 | 이미지 크기, 채널 수, 행 단위 바이트 수, 픽셀 데이터 접근, 픽셀 접근, 빈 이미지 판정을 구현한다. |
| 2 | 이미지 컨테이너 기능 확장 | 깊은 복사, 재생성, 전체 채우기, 크기와 shape 비교, `Size`, `Point`, `Rect`, ROI 검증을 구현한다. |
| 3 | 기본 이미지 유틸리티 | 채우기, 복사, 절대 차이, non-zero 픽셀 수, 이미지 동일성 비교, 최대 픽셀 차이 계산을 구현한다. |
| 4 | 기반 기능 보강과 예제 | 앞선 기능의 오류를 검증하고 미비한 API를 보완한 뒤, `Mat` 사용 흐름을 보여주는 작은 예제를 만든다. |

### 2단계: 이미지 입출력

| 주차 | 주제 | 핵심 내용 |
| ---: | --- | --- |
| 5 | 그레이스케일 이미지 입출력 | PGM `P2`, `P5` 형식의 읽기와 저장을 구현하고 저장한 이미지를 다시 읽어 검증한다. |
| 6 | RGB 이미지 입출력 | PPM `P3`, `P6` 형식의 읽기와 저장을 구현하고 RGB 채널 순서를 검증한다. |
| 7 | 통합 이미지 입출력 | 확장자 기반 읽기/저장 선택과 Netpbm 헤더 공통 파서를 구현한다. |
| 8 | 입출력 보강과 예제 | 파싱 오류와 파일 오류 처리를 보완한 뒤, 저장 후 다시 읽어 비교하는 예제를 만든다. |

### 3단계: 픽셀 연산과 히스토그램

| 주차 | 주제 | 핵심 내용 |
| ---: | --- | --- |
| 9 | 색상 변환과 채널 처리 | RGB/그레이스케일 변환, RGB/BGR 채널 교환, 채널 추출/분리/병합을 구현한다. |
| 10 | 기본 픽셀 연산과 영역 처리 | 반전, 밝기/대비 조정, 바이트 범위 제한, crop, 영역 복사/붙여넣기를 구현한다. |
| 11 | 히스토그램과 정규화 | 그레이스케일 히스토그램, CDF, 밝기 최솟값/최댓값, min-max 정규화를 구현한다. |
| 12 | 픽셀 연산과 히스토그램 보강 | 픽셀 연산과 히스토그램 결과를 작은 이미지로 검증하고 미비한 처리를 보완한 뒤, 히스토그램 예제를 만든다. |

### 4단계: 임계값 처리와 필터링 기초

| 주차 | 주제 | 핵심 내용 |
| ---: | --- | --- |
| 13 | 대비 개선과 임계값 처리 | 대비 스트레칭, 히스토그램 평활화, 임계값 처리 계열 연산을 구현한다. |
| 14 | Otsu와 적응형 임계값 | Otsu 임계값, 적분 이미지, 사각형 영역 합, 적응형 평균 임계값을 구현한다. |
| 15 | 경계 처리와 컨볼루션 | constant/replicate 경계 처리와 2D 컨볼루션을 구현하고 작은 kernel로 검증한다. |
| 16 | 임계값 처리와 컨볼루션 보강 | 임계값 처리와 컨볼루션 결과를 검증하고 미비한 처리를 보완한 뒤, 결과 이미지를 저장하는 예제를 만든다. |

### 5단계: 필터링과 엣지 검출

| 주차 | 주제 | 핵심 내용 |
| ---: | --- | --- |
| 17 | 블러와 선명화 | 박스 블러, 가우시안 블러, 가우시안 커널, 선명화, Laplacian response를 구현한다. |
| 18 | 중앙값 필터와 signed response | 중앙값 필터와 signed response 시각화를 구현한다. |
| 19 | Sobel 엣지 | Sobel X/Y, gradient magnitude, gradient direction, edge map normalization을 구현한다. |
| 20 | 필터링과 엣지 보강 | 경계 처리와 signed response를 검증하고 미비한 부분을 보완한 뒤, 필터링과 엣지 결과를 비교하는 예제를 만든다. |

### 6단계: 모폴로지와 객체 영역 분석

| 주차 | 주제 | 핵심 내용 |
| ---: | --- | --- |
| 21 | 모폴로지 | 구조 요소(structuring element), erosion, dilation, opening, closing을 구현한다. |
| 22 | 객체 영역 분석 | 연결 영역 라벨링을 구현하고 각 영역의 면적, 경계 박스, 중심점을 계산한다. |
| 23 | 기본 기하 변환 | 최근접/양선형 크기 변경, 좌우/상하 반전, 90도 회전을 구현한다. |
| 24 | 영역 분석 보강과 예제 | 임계값 처리, 모폴로지, 연결 영역 분석 흐름을 검증하고 미비한 부분을 보완한 뒤, 이진 이미지에서 객체 후보를 찾는 예제를 만든다. |

### 최종 통합

| 주차 | 주제 | 핵심 내용 |
| ---: | --- | --- |
| 25 | 통합 예제 구성 | 이미지 읽기, 변환, 임계값 처리, 필터링, 엣지 검출, 모폴로지, 라벨링을 하나의 처리 흐름으로 연결한다. |
| 26 | 최종 품질 점검 | 전체 테스트와 예제를 macOS와 Windows에서 실행하고 API, 파일 배치, 경고, README를 정리한다. |

## 기대 상태

miniCV는 단순한 연습 코드가 아니라, 작은 이미지를 읽고 처리하고 분석하는 흐름을 끝까지 수행할 수 있는 C++ 이미지 처리 라이브러리를 지향한다.

- `Mat` 기반 이미지 표현, 픽셀 접근, ROI, 복사, 재생성, PGM/PPM 입출력을 안정적으로 지원한다.
- 색상 변환, 채널 처리, 밝기/대비 조정, 히스토그램, 임계값 처리, Otsu 임계값을 구현한다.
- 컨볼루션과 경계 처리를 바탕으로 블러, 선명화, 중앙값 필터, Laplacian, Sobel edge를 구현한다.
- 모폴로지, 연결 영역 분석, 기본 기하 변환을 이용해 단순한 객체 영역 분석 흐름을 구성한다.
- 기능별 테스트와 통합 예제로 구현 결과를 재현 가능하게 검증한다.
